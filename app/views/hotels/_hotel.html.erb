<div id="<%= dom_id @hotel %>">
  <p>
    <strong>Name:</strong>
    <%= @hotel.name %>
  </p>
  <p>
    <strong>Rooms:</strong>
    <% @hotel.rooms.each do |room| %>
    <%= room.room_no %>
    <% end %>
  </p>

</div>
