<p style="color: green"><%= notice %></p>

<h1>Hotel groups</h1>

<div id="hotel_groups">
  <% @hotel_groups.each do |hotel_group| %>
    <%= render hotel_group %>
    <p>
      <%= link_to "Show this hotel group", hotel_group %>
    </p>
  <% end %>
</div>

<%= link_to "New hotel group", new_hotel_group_path %>
